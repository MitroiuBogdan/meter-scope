# grafana/provisioning/datasources/prometheus-datasource.yaml
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    url: http://prometheus:9090 # This is the crucial line
    access: proxy
    isDefault: true
    editable: true